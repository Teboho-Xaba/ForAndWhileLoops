const names = ['Ed', 'John', 'Maria', 'Ted', 'Yuji', 'Sukuna', 'Higuruma']

for (name of names){
    if(name === 'Martha'){
    console.log('Maria is in my list.')
    }
    else {
        console.log('Check the name spelling')
    }

};

const loading = 0

while (loading < 100){
    console.log('Website is still loading')
    loading++
}